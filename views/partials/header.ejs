<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> | Carnet de Stage</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
</head>
<body>
    <header class="site-header">
        <div class="logo"><a href="/">Carnet de Stage</a></div>

        <button class="nav-toggle" aria-label="toggle navigation">
            <span class="hamburger"></span>
        </button>

        <nav class="main-nav">
            <a href="/" class="<%= activePage === 'accueil' ? 'active' : '' %>">Accueil</a>
            <a href="/profil" class="<%= activePage === 'profil' ? 'active' : '' %>">Mon Profil</a>
            <a href="/stage" class="<%= activePage === 'stage' ? 'active' : '' %>">Mon Stage</a>
            <a href="/journal" class="<%= activePage === 'journal' ? 'active' : '' %>">Journal de Bord</a>
            
            <% if (locals.username) { %>
                <a href="/deconnexion" class="<%= activePage === 'admin' ? 'active' : '' %>">Déconnexion (<%= locals.username %>)</a>
            <% } else { %>
                <a href="/connexion" class="<%= activePage === 'admin' ? 'active' : '' %>">Administration</a>
            <% } %>
        </nav>

        <div id="google_translate_element" class="google-translate-widget"></div>

    </header>
    <div class="container">