<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> | Carnet de Stage</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flag-icon-css/7.2.1/css/flag-icons.min.css">
</head>
<body>
    <header class="site-header">
        <div class="logo"><a href="/">Carnet de Stage</a></div>

        <button class="nav-toggle" aria-label="toggle navigation">
            <span class="hamburger"></span>
        </button>

        <nav class="main-nav">
            <a href="/" class="<%= activePage === 'accueil' ? 'active' : '' %>"><%= t('nav.home') %></a>
            <a href="/profil" class="<%= activePage === 'profil' ? 'active' : '' %>"><%= t('nav.profile') %></a>
            <a href="/stage" class="<%= activePage === 'stage' ? 'active' : '' %>"><%= t('nav.internship') %></a>
            <a href="/journal" class="<%= activePage === 'journal' ? 'active' : '' %>"><%= t('nav.journal') %></a>
            <a href="/contact" class="<%= activePage === 'contact' ? 'active' : '' %>">Contact</a> 
        </nav>

            <% if (locals.username) { %>
                <a href="/deconnexion" class="<%= activePage === 'admin' ? 'active' : '' %>"><%= t('nav.logout') %> (<%= locals.username %>)</a>
            <% } else { %>
                <a href="/connexion" class="<%= activePage === 'admin' ? 'active' : '' %>"><%= t('nav.admin') %></a>
            <% } %>
        </nav>

        <form action="/search" method="GET" class="search-form">
            <input type="search" name="query" placeholder="Rechercher..." aria-label="Rechercher dans le journal">
            <button type="submit">🔍</button>
        </form>
        
        <div class="language-switcher" style="margin-left: 1.5rem; display: flex; align-items: center; gap: 0.5rem;">
            <a href="?lng=fr" title="Français" style="opacity: <%= i18n.language === 'fr' ? '1' : '0.6' %>;">
                <span class="fi fi-fr"></span>
            </a>
            <span style="color: var(--c-text-light);">|</span>
            <a href="?lng=en" title="English" style="opacity: <%= i18n.language === 'en' ? '1' : '0.6' %>;">
                <span class="fi fi-gb"></span>
            </a>
        </div>
        </header>
    <div class="container">