<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> | Carnet de Stage</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <link rel="stylesheet" href="https://unpkg.com/easymde/dist/easymde.min.css">
</head>
<body>
    <header class="site-header">
        <div class="logo"><a href="/">Carnet de Stage</a></div>

        <button class="nav-toggle" aria-label="toggle navigation">
            <span class="hamburger"></span>
        </button>

        <nav class="main-nav">
            <a href="/" class="<%= activePage === 'accueil' ? 'active' : '' %>"><%= t('nav.home') %></a>
            <a href="/profil" class="<%= activePage === 'profil' ? 'active' : '' %>"><%= t('nav.profile') %></a>
            <a href="/stage" class="<%= activePage === 'stage' ? 'active' : '' %>"><%= t('nav.internship') %></a>
            <a href="/journal" class="<%= activePage === 'journal' ? 'active' : '' %>"><%= t('nav.journal') %></a>

            <% if (locals.username) { %>
                <a href="/deconnexion" class="<%= activePage === 'admin' ? 'active' : '' %>"><%= t('nav.logout') %> (<%= locals.username %>)</a>
            <% } else { %>
                <a href="/connexion" class="<%= activePage === 'admin' ? 'active' : '' %>"><%= t('nav.admin') %></a>
            <% } %>
        </nav>
        
        <div class="language-switcher" style="margin-left: 1.5rem;">
            <a href="?lng=fr" style="color: var(--c-text); font-weight: bold; text-decoration: <%= i18n.language === 'fr' ? 'underline' : 'none' %>;">FR</a>
            <span style="color: var(--c-text-light);">|</span>
            <a href="?lng=en" style="color: var(--c-text); font-weight: bold; text-decoration: <%= i18n.language === 'en' ? 'underline' : 'none' %>;">EN</a>
        </div>

        </header>
    <div class="container">