<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t('maintenance.title') %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flag-icon-css/7.2.1/css/flag-icons.min.css">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            text-align: center;
            background-color: var(--c-background);
            color: var(--c-text);
        }
        .maintenance-container {
            max-width: 600px;
            padding: 2rem;
            background: var(--c-surface);
            border-radius: 12px;
            border: 1px solid var(--c-border);
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        h1 { margin-top: 0; color: var(--c-primary); }
        i { font-size: 4rem; color: var(--c-text-light); margin-bottom: 1rem; }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
</head>
<body>
    <div class="maintenance-container">
        <i class="fas fa-tools"></i>
        <h1><%= t('maintenance.title') %></h1>

        <p><%= t('maintenance.desc_1') %></p>
        <p><%= t('maintenance.desc_2') %></p>

        <hr>

        <p style="margin-top: 1rem; font-size: 0.9rem; color: var(--c-text-light);">
            <%= t('maintenance.thanks') %>
        </p>

        <div class="lang-links" style="margin-top: 2rem;">
            <a href="?lng=fr"><span class="fi fi-fr"></span></a> | <a href="?lng=en"><span class="fi fi-gb"></a>
        </div>
    </div>
</body>
</html>